import numpy as np

def mimatmul(A,B):
    N = len(A)
    matriz = (N,N)
    C = np.zeros(matriz)
    
    for i in range(len(A)): 
        for j in range(len(B[])):
            for k in range(len(B)):
                C[i][k]+= A[i][j]*B[j][k]
    return C                
